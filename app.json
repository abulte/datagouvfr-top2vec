{
  "scripts": {
    "dokku": {
      "predeploy": "mkdir -p models && curl -L 'https://huggingface.co/abulte/top2vec-datagouvfr/resolve/main/top2vec_distiluse-base-multilingual-cased.bin?download=true' -o models/top2vec_distiluse-base-multilingual-cased.bin"
    }
  }
}
